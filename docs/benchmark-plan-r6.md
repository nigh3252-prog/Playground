# Watershed r6 — benchmark calibration + parent-world crops

This branch builds on the r5 real-data reference and basin-water work. The goal is to stop relying on visual intuition alone: generated worlds should be compared against real reference regions, while generated 1,200 km views come from a larger parent world rather than a fixed ocean/mountain frame.

Implementation in progress on this branch.
